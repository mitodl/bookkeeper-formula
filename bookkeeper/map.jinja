{% set bookkeeper = salt.grains.filter_by({
    'default': {
        'pkgs': ['bookkeeper'],
        'service': 'bookkeeper',
        'conf_file': '/etc/bookkeeper/bookkeeper.conf',
        'config': {
            'key': 'value',
        },
    },
    'Debian': {
    },
    'RedHat': {
    },
}, grain='os_family', merge=salt.pillar.get('bookkeeper:overrides'), base='default') %}
